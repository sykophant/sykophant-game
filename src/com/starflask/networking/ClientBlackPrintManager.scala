package com.starflask.networking


/*
 *  Receives a message from the server that is the hash of the blackprint the server is using
 *  If the client does not have a print with that hash, the client will request the blackprint
 *  The client will then check for the map hash (in the blackprint) and will download that file if necessary
 *  
 *  These files are stores in appdata/sykophant 
 * 
 * 
 */
class ClientBlackPrintManager {
  
}